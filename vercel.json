{
  "buildCommand": "cd frontend && npm run build && cd .. && rm -rf .vercel && cp -r frontend/.vercel .",
  "outputDirectory": ".vercel/output",
  "rewrites": [
    {
      "source": "/api-proxy/:path*",
      "destination": "http://184.107.179.209:3333/v1/:path*"
    }
  ]
}
