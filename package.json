{
  "name": "unisism",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.0",
    "postcss": "^8.5.10",
    "tailwindcss": "^4.2.4"
  }
}
